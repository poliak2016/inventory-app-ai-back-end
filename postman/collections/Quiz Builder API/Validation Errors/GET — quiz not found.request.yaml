$kind: http-request
url: "{{baseUrl}}/quizzes/nonexistent-id-000"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 404', () => pm.response.to.have.status(404));
      pm.test('success false', () => pm.expect(pm.response.json().success).to.be.false);
    language: text/javascript
order: 8000
